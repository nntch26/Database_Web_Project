document.getElementById('apply-filter').addEventListener('click', function() {
    // รับค่าที่เลือกจากตัวกรอง Rating
    var selectedRating = document.getElementById('rating').value;
    
    // ส่งค่าไปยังหน้า PHP ที่ดึงข้อมูลโรงแรม
    // และส่งค่าตัวกรอง Rating ไปด้วย
    // หรืออัพเดต URL ของหน้าเว็บเพื่อร้องข้อมูลใหม่
    // โดยใช้ JavaScript หรือโหลดหน้าเว็บใหม่
});

